{
  "name": "strength-training-graphs",
  "version": "0.1.0",
  "description": "Obsidian plugin that plots strength training metrics from frontmatter.",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.20.2",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "obsidian": "latest"
  }
}