# Changelog

## Unreleased

- Fixed mobile chart layout so the graph keeps its own space and scrolls cleanly on narrow screens.
- Improved PNG export with chart title, axis titles, wrapped legend, and higher-contrast white axis/marker styling for dark backgrounds.
- Improved chart tooltips so labels are bold, values are easier to read, and popups stay visible near screen edges.
